name: Unit Tests

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install ShellSpec
        run: |
          curl -fsSL https://git.io/shellspec | sh -s -- --yes
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          
      - name: Setup Test Environment
        run: |
          mkdir -p spec/unit/lib
          cp scripts/lib/*.sh spec/unit/lib/
          cp spec/unit/lib/*.sh spec/unit/lib/ || true
          chmod +x spec/unit/lib/*.sh
          ls -la spec/unit/lib/
          
      - name: Run Unit Tests
        run: |
          cd $GITHUB_WORKSPACE
          export PATH="$HOME/.local/bin:$PATH"
          shellspec --init
          shellspec --format documentation --shell bash spec/unit/lib/init_spec.sh spec/unit/lib/parse_parameters_spec.sh || exit 1
      
      - name: Generate Coverage Report
        run: |
          cd $GITHUB_WORKSPACE
          shellspec --coverage --kcov-options "--include-pattern=scripts/" spec/unit/lib/
      
      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          
      - name: Report Coverage
        run: |
          echo "Coverage Report Summary:"
          cat coverage/coverage.txt || echo "No coverage data available"
