---
name: PR Build & Test

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  BUILDKIT_PROGRESS: plain
  BUILDKIT_COLORS: 1
  BUILDKIT_STEP_LOG_MAX_SIZE: 10485760
  BUILDKIT_STEP_LOG_MAX_SPEED: 10485760
  DOCKER_BUILDKIT: 1
  DOCKER_SCAN_SUGGEST: false
  BUILDX_NO_DEFAULT_LOAD: true
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/rover

permissions:
  contents: read
  packages: write
  checks: write
  pull-requests: write
  actions: read
  id-token: write
  statuses: write
  security-events: write

defaults:
  run:
    shell: bash

jobs:
  matrix-prep:
    runs-on: ubuntu-latest
    outputs:
      platforms: ${{ steps.platforms.outputs.matrix }}
      agents: ${{ steps.agents.outputs.matrix }}
    steps:
      - id: platforms
        run: |
          echo "matrix=[\"linux/amd64\", \"linux/arm64\"]" >> $GITHUB_OUTPUT
      - id: agents
        run: |
          echo "matrix=[\"github\", \"tfc\", \"gitlab\", \"azure_devops\"]" >> $GITHUB_OUTPUT

  build-base:
    needs: matrix-prep
    runs-on: ubuntu-latest
    outputs:
      github_runner: ${{ steps.versions.outputs.github_runner }}
      azdo: ${{ steps.versions.outputs.azdo }}
      tfc: ${{ steps.versions.outputs.tfc }}
      docker_compose: ${{ steps.versions.outputs.docker_compose }}
      golang: ${{ steps.versions.outputs.golang }}
      ansible: ${{ steps.versions.outputs.ansible }}
      azure_cli: ${{ steps.versions.outputs.azure_cli }}
    strategy:
      matrix:
        platform: ${{ fromJson(needs.matrix-prep.outputs.platforms) }}
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}/rover
          tags: |
            type=sha,prefix=pr-
            type=ref,event=pr

      - name: Parse versions
        id: versions
        run: |
          echo "vault=$(./scripts/parse_versions.sh tool vault)" >> $GITHUB_OUTPUT
          echo "golang=$(./scripts/parse_versions.sh tool golang)" >> $GITHUB_OUTPUT
          echo "kubectl=$(./scripts/parse_versions.sh tool kubectl)" >> $GITHUB_OUTPUT
          echo "kubelogin=$(./scripts/parse_versions.sh tool kubelogin)" >> $GITHUB_OUTPUT
          echo "docker_compose=$(./scripts/parse_versions.sh tool docker_compose)" >> $GITHUB_OUTPUT
          echo "terraform_docs=$(./scripts/parse_versions.sh tool terraform_docs)" >> $GITHUB_OUTPUT
          echo "packer=$(./scripts/parse_versions.sh tool packer)" >> $GITHUB_OUTPUT
          echo "powershell=$(./scripts/parse_versions.sh tool powershell)" >> $GITHUB_OUTPUT
          echo "ansible=$(./scripts/parse_versions.sh tool ansible)" >> $GITHUB_OUTPUT
          echo "azure_cli=$(./scripts/parse_versions.sh tool azure_cli)" >> $GITHUB_OUTPUT
          echo "terrascan=$(./scripts/parse_versions.sh tool terrascan)" >> $GITHUB_OUTPUT
          echo "tfupdate=$(./scripts/parse_versions.sh tool tfupdate)" >> $GITHUB_OUTPUT
          echo "terraform=$(./scripts/parse_versions.sh terraform | head -n1)" >> $GITHUB_OUTPUT

      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          network: host
          allow: |
            network.host
            security.insecure
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}/rover:pr-${{ github.sha }}-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
            ${{ env.REGISTRY }}/${{ github.repository }}/rover:pr-latest-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          target: base
          platforms: ${{ matrix.platform }}
          cache-from: |
            type=gha,scope=pr-${{ github.event.pull_request.number }}-${{ matrix.platform }}-${{ github.sha }},mode=max
            type=gha,scope=main-${{ matrix.platform }},mode=max
          cache-to: |
            type=gha,mode=max,scope=pr-${{ github.event.pull_request.number }}-${{ matrix.platform }}-${{ github.sha }},compression=zstd
          outputs: |
            type=registry,compression=zstd,compression-level=3,force-compression=true
            type=image,compression=zstd,compression-level=3,force-compression=true
          provenance: false
          sbom: false
          build-args: |
            TARGETARCH=${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
            TARGETOS=linux
            USER_UID=1000
            USER_GID=1000
            USERNAME=vscode
            versionVault=${{ steps.versions.outputs.vault }}
            versionGolang=${{ steps.versions.outputs.golang }}
            versionKubectl=${{ steps.versions.outputs.kubectl }}
            versionKubelogin=${{ steps.versions.outputs.kubelogin }}
            versionDockerCompose=${{ steps.versions.outputs.docker_compose }}
            versionTerraformDocs=${{ steps.versions.outputs.terraform_docs }}
            versionPacker=${{ steps.versions.outputs.packer }}
            versionPowershell=${{ steps.versions.outputs.powershell }}
            versionAnsible=${{ steps.versions.outputs.ansible }}
            extensionsAzureCli=${{ steps.versions.outputs.azure_cli }}
            versionTerrascan=${{ steps.versions.outputs.terrascan }}
            versionTfupdate=${{ steps.versions.outputs.tfupdate }}
            versionTerraform=${{ steps.versions.outputs.terraform }}
      - name: Move cache
        if: always()
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache || true

  build-agents:
    needs: [build-base, matrix-prep]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: ${{ fromJson(needs.matrix-prep.outputs.platforms) }}
        agent: ${{ fromJson(needs.matrix-prep.outputs.agents) }}
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push agent
        id: build-agent
        uses: docker/build-push-action@v5
        with:
          context: .
          network: host
          allow: |
            network.host
            security.insecure
          file: ./agents/${{ matrix.agent }}/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ matrix.agent }}-${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ matrix.agent }}-latest
          platforms: ${{ matrix.platform }}
          cache-from: |
            type=gha,scope=pr-${{ github.event.pull_request.number }}-agent-${{ matrix.agent }},mode=max
            type=gha,scope=main-agent-${{ matrix.agent }},mode=max
          cache-to: |
            type=gha,mode=max,scope=pr-${{ github.event.pull_request.number }}-agent-${{ matrix.agent }}-${{ github.sha }},compression=zstd
          outputs: |
            type=registry,compression=zstd,compression-level=3,force-compression=true
            type=image,compression=zstd,compression-level=3,force-compression=true
          build-contexts: |
            base=docker-image://${{ env.REGISTRY }}/${{ github.repository }}/rover:pr-${{ github.sha }}-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          provenance: false
          sbom: false
          build-args: |
            REGISTRY=${{ env.REGISTRY }}
            GITHUB_REPOSITORY=${{ github.repository }}
            GITHUB_SHA=${{ github.sha }}
            TARGETARCH=${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
            TARGETOS=linux
            USERNAME=vscode
            versionGithubRunner=${{ needs.build-base.outputs.github_runner }}
            versionAzdo=${{ needs.build-base.outputs.azdo }}
            versionTfc=${{ needs.build-base.outputs.tfc }}
            versionDockerCompose=${{ needs.build-base.outputs.docker_compose }}
            versionGolang=${{ needs.build-base.outputs.golang }}
            versionAnsible=${{ needs.build-base.outputs.ansible }}
            extensionsAzureCli=${{ needs.build-base.outputs.azure_cli }}

  trivy-scan:
    needs: [build-base, build-agents]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
      fail-fast: false
    steps:
      - name: Run Trivy scan for base image
        uses: aquasecurity/trivy-action@master
        with:
          image: ${{ env.REGISTRY }}/${{ github.repository }}/rover:pr-${{ github.sha }}-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          format: 'sarif'
          output: 'trivy-results-base-${{ matrix.platform }}.sarif'
          severity: 'CRITICAL,HIGH'
          
      - name: Upload base image scan results
        uses: github/codeql-action/upload-sarif@v3
        if: success() || failure()
        with:
          sarif_file: 'trivy-results-base-${{ matrix.platform }}.sarif'
          category: 'Trivy-Base-${{ matrix.platform }}'
          wait-for-processing: true
          
      - name: Run Trivy scans for agents
        uses: aquasecurity/trivy-action@master
        with:
          image: |
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-github
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-tfc
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-gitlab
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-azure_devops
          format: 'sarif'
          output: 'trivy-results-agents-${{ matrix.platform }}.sarif'
          severity: 'CRITICAL,HIGH'
          
      - name: Upload agent scan results
        uses: github/codeql-action/upload-sarif@v3
        if: success() || failure()
        with:
          sarif_file: 'trivy-results-agents-${{ matrix.platform }}.sarif'
          category: 'Trivy-Agents-${{ matrix.platform }}'
          wait-for-processing: true

  msdo-scan:
    needs: [build-base, build-agents]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      
      - name: Run MSDO scan for base image
        uses: microsoft/security-devops-action@v1
        id: msdo-base
        with:
          categories: containers
          policy: GitHub
          tools: trivy
          github-token: ${{ secrets.GITHUB_TOKEN }}
          image: ${{ env.REGISTRY }}/${{ github.repository }}/rover:pr-${{ github.sha }}-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          
      - name: Upload MSDO scan results for base image
        uses: github/codeql-action/upload-sarif@v3
        if: success() || failure()
        with:
          sarif_file: ${{ steps.msdo-base.outputs.sarifFile }}
          category: 'MSDO-Base-${{ matrix.platform }}'
          wait-for-processing: true
          
      - name: Run MSDO scan for agents
        uses: microsoft/security-devops-action@v1
        id: msdo-agents
        with:
          categories: containers
          policy: GitHub
          tools: trivy
          github-token: ${{ secrets.GITHUB_TOKEN }}
          image: |
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-github
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-tfc
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-gitlab
            ${{ env.REGISTRY }}/${{ github.repository }}/rover-agent:pr-${{ github.sha }}-${{ matrix.platform }}-azure_devops
          
      - name: Upload MSDO scan results for agents
        uses: github/codeql-action/upload-sarif@v3
        if: success() || failure()
        with:
          sarif_file: ${{ steps.msdo-agents.outputs.sarifFile }}
          category: 'MSDO-Agents-${{ matrix.platform }}'
          wait-for-processing: true


  build-report:
    needs: [build-base, build-agents, trivy-scan, msdo-scan]
    permissions:
      checks: write
      pull-requests: write
      contents: read
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        
      - name: Generate build report
        uses: actions/github-script@v7
        env:
          BUILD_BASE_RESULT: ${{ needs.build-base.result }}
          BUILD_AGENTS_RESULT: ${{ needs.build-agents.result }}
          TRIVY_SCAN_RESULT: ${{ needs.trivy-scan.result }}
          MSDO_SCAN_RESULT: ${{ needs.msdo-scan.result }}
        with:
          script: |
            const { owner, repo } = context.repo;
            const { number } = context.issue;
            const buildBaseResult = process.env.BUILD_BASE_RESULT;
            const buildAgentsResult = process.env.BUILD_AGENTS_RESULT;
            const trivyScanResult = process.env.TRIVY_SCAN_RESULT;
            const msdoScanResult = process.env.MSDO_SCAN_RESULT;
            
            const buildSummary = [
              '# Build & Security Report',
              '',
              '## Base Image Builds',
              `- linux/amd64: ${buildBaseResult === 'success' ? '✅' : '❌'}`,
              `- linux/arm64: ${buildBaseResult === 'success' ? '✅' : '❌'}`,
              '',
              '## Agent Builds',
              `- github: ${buildAgentsResult === 'success' ? '✅' : '❌'}`,
              `- tfc: ${buildAgentsResult === 'success' ? '✅' : '❌'}`,
              `- gitlab: ${buildAgentsResult === 'success' ? '✅' : '❌'}`,
              `- azure_devops: ${buildAgentsResult === 'success' ? '✅' : '❌'}`,
              '',
              '## Security Scans',
              `- Trivy vulnerability scanning: ${trivyScanResult === 'success' ? '✅' : '❌'}`,
              `- MSDO security analysis: ${msdoScanResult === 'success' ? '✅' : '❌'}`,
              `- SARIF reports processed: ${trivyScanResult === 'success' && msdoScanResult === 'success' ? '✅' : '❌'}`,
              '',
              `Overall Status: ${buildBaseResult === 'success' && 
                                buildAgentsResult === 'success' && 
                                trivyScanResult === 'success' && 
                                msdoScanResult === 'success' ? '✅' : '❌'}`
            ].join('\n');
            
            if (context.payload.pull_request) {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: number,
                body: buildSummary
              });
            }
            
            await github.rest.checks.create({
              owner,
              repo,
              name: 'Build & Security Check',
              head_sha: context.sha,
              status: 'completed',
              conclusion: buildBaseResult === 'success' && 
                         buildAgentsResult === 'success' && 
                         trivyScanResult === 'success' && 
                         msdoScanResult === 'success' ? 'success' : 'failure',
              output: {
                title: `Build Report ${buildBaseResult === 'success' && 
                                    buildAgentsResult === 'success' && 
                                    trivyScanResult === 'success' && 
                                    msdoScanResult === 'success' ? '✅' : '❌'}`,
                summary: buildSummary
              }
            });
